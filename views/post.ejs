<link rel="stylesheet" href="/css/post.css"> 


<div id="post">
    <li >
        <div id="post-info" >
            <div id="post-info-name";>
               <a href="/users/profile/<%= post.user.id%>" style="text-decoration:none;color:#0A65C2;"><b><%= post.user.name %></b></a>
               <%if (locals.user && locals.user.id == post.user.id) {%>
               <small>
                <%= post.createdAt.getDate()+"-"+(post.createdAt.getMonth()+1)+"-"+post.createdAt.getFullYear()%>
               </small>
               <%}%>
            </div>
                    <small id="post-info-time">
                        <a href="/posts/destroy/<%= post.id %>">delete</a>
                    </small>
        </div>
        <div id="post-content">
            <%= post.content%>
        </div>
        <hr>
        <br>
        <div id="post-comment">
            <%- include('posts_comment_form')%>
        </div>
        <%if(post.comments) { %>
            <%- include('comment')%>
        <%}%>

    </li>
</div>